<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GrindIt_.Views"
             x:DataType="local:StepsCard"
             x:Class="GrindIt_.Views.StepsCard">
    <Border
        Stroke="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource CodGray}}"
        BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource CodGray}}"
        StrokeShape="30"
        Margin="10,10,10,5">
        <Border.Shadow>
            <Shadow
                Brush="{StaticResource Black}"
                Offset="4,4"
                Radius="4"
                Opacity=".25"/>
        </Border.Shadow>

        <Grid ColumnDefinitions="*,*">
            <Grid
                Margin="15,15,10,10"
                RowDefinitions="Auto, Auto">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label 
                    Text="{Binding Value}"
                    TextColor="{AppThemeBinding Light=Black, Dark=White}"
                    FontSize="{OnIdiom Phone=20, Tablet=20}" 
                    Margin="5"
                    VerticalOptions="Center"
                    HorizontalOptions="StartAndExpand"
                    Grid.Column="0"/>
                <Label
                    Grid.Row="1"
                    Grid.Column="0"
                    Text="{Binding TargetValue}"
                    TextColor="{AppThemeBinding Light=Black, Dark=White}"
                    FontSize="{OnIdiom Phone=22, Tablet=22}"/>
            </Grid>
            <Border
                HeightRequest="100"
                WidthRequest="100"
                Stroke="Transparent"
                HorizontalOptions="End"
                Margin="10,0,20,0"
                Grid.Column="1">
                    <Image />
            </Border>
        </Grid>
    </Border>
</ContentView>
