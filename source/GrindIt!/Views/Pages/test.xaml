<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GrindIt_.Views.Pages.test"
             xmlns:converters="clr-namespace:GrindIt_.Resources.Converters"
             Title="test">
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:IntensityToColorConverter x:Key="IntensityToColorConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Button
        x:Name="Return"
        Text="return"
        Clicked="Return_Clicked"/>
    <!--
    <CollectionView ItemsSource="{Binding WorkoutDays}" SelectionMode="None">
        <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Horizontal" Span="7" />
        </CollectionView.ItemsLayout>

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame WidthRequest="20" HeightRequest="20"
                       CornerRadius="5"
                       BackgroundColor="{Binding Intensity, Converter={StaticResource IntensityToColorConverter}}">
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    -->
</ContentPage>